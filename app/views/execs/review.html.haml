.container
  %h1 Welcome, #{current_user.email}. You have ranked #{current_user.hacker_ranking.count} hackers
  %h2 HACKER REVIEW
  %table{:border => 1, class: 'table'}
    %tbody
      %tr
        %td Name
        %td School
        %td Number of reviews
      - @hackers.each do |object|
        %tr
          %td
            %a{href: "hacker_detail/#{object.id}"}= "#{object.first_name} #{object.last_name}"
          %td= "#{object.school.name}, #{object.school.state}, #{object.school.country} ( #{object.school.category})"
          %td= "#{object.hacker_ranking.count}"